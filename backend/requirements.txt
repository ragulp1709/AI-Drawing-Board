# Pinned for mutual compatibility: mediapipe 0.10.x requires numpy<2,
# and opencv-python 4.9.x is the last release compatible with numpy 1.x.
opencv-python==4.9.0.80
mediapipe>=0.10.0,<0.11
numpy>=1.24.0,<2.0

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
